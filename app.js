const f=document.getElementById("files"),p=document.getElementById("preview"),c=document.getElementById("count"),l=document.getElementById("log");f.onchange=e=>{p.innerHTML="";const files=[...e.target.files];c.textContent=`已导入：${files.length} 张照片`;files.forEach(file=>{if(file.type.startsWith("image/")){const img=document.createElement("img");img.src=URL.createObjectURL(file);p.appendChild(img);}})};document.getElementById("start").onclick=()=>{l.textContent="这是 V1 演示版。\n后续将加入 RAW 解码、星点检测、自动对齐、堆栈算法。";};